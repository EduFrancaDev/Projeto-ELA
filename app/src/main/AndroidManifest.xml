<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appandroid">

    <!-- ✅ Permissão obrigatória para Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAndroid"
        tools:targetApi="31">

        <!-- 📲 Tela inicial (cadastro/login) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🟣 Tela principal do chamado -->
        <activity android:name=".TelaPrincipal" />

        <!-- 📘 Histórico de chamados -->
        <activity android:name=".HistoricoActivity" />

        <!-- 👤 Tela de perfil -->
        <activity android:name=".PerfilActivity" />

        <!-- 🛡️ Tela de contatos de segurança -->
        <activity android:name=".ContatosSegurancaActivity" />

        <!-- 🔔 BroadcastReceivers para notificações -->
        <receiver android:name=".ConfirmReceiver" />
        <receiver android:name=".CancelReceiver" />
        <receiver android:name=".AutoConfirmReceiver" />

    </application>

</manifest>
